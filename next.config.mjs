/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Add any external domains here if you host your avatar remotely
    remotePatterns: [],
  },
};

export default nextConfig;
